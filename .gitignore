# No dot-directories except github/vscode/.yarn
.*/
!.vscode/
!.github/
!.yarn/

# Yarn config and stuff - we're not using Zero-Installs
# because that would blow up the repo size even more
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions
.pnp.*

node_modules
package-lock.json
*.tgz

# Compiled source files
**/build
**/*.tsbuildinfo
*.map

# Windows stuff
Thumbs.db
